# ==============================================================================
# Trivy risk-acceptance list (spec 20.7 "allow/deny exceptions with expiration").
# Every entry needs: justification, owner, expiry (exp:YYYY-MM-DD — Trivy stops ignoring the
# finding after that date, so the blocking scans fail again and force a re-review).
# Tracked in SECURITY_REVIEW.md §5 "Open issues".
# ==============================================================================

# (no active exceptions — the postcss 8.4.31 entries were removed when `next>postcss` was
# overridden to ^8.5.23 in package.json; SECURITY_REVIEW.md S7)
