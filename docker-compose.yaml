# ==============================================================================
# ERP Preflight — Production Coolify / Hostinger Docker Compose Orchestration
# ==============================================================================
# Target Domain: erppreflight.com / api.erppreflight.com
# Compatible with Coolify v4+, Hostinger VPS (Ubuntu 22.04 / 24.04 LTS), Traefik
# Topology Matrix:
#   - web:              Port 3000 (Next.js 15 App Router Frontend)
#   - api:              Port 3001 (NestJS 11 Core SaaS API Backend)
#   - analysis-python:  Port 8000 (Python 3.13 FastAPI Stateless Microservice)
#   - postgres:         Port 5432 (PostgreSQL 16 + pgvector)
#   - redis:            Port 6379 (Redis 7.2 Alpine)
#   - minio:            Port 9000 (S3 API), 9001 (Console)
# ==============================================================================

services:
  # ----------------------------------------------------------------------------
  # PostgreSQL 16 with pgvector extension & RLS
  # ----------------------------------------------------------------------------
  postgres:
    image: pgvector/pgvector:pg16
    container_name: erppreflight-postgres
    restart: unless-stopped
    expose:
      - "5432"
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-erppreflight}
      POSTGRES_USER: ${POSTGRES_USER:-erppreflight}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-erppreflight_secret}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres || pg_isready -U $$POSTGRES_USER -d $$POSTGRES_DB"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2048M
        reservations:
          cpus: '0.5'
          memory: 512M
    networks:
      - erppreflight-net

  # ----------------------------------------------------------------------------
  # Redis 7 (Cache, Distributed Locks & BullMQ Job Queues)
  # ----------------------------------------------------------------------------
  redis:
    image: redis:7.2-alpine
    container_name: erppreflight-redis
    restart: unless-stopped
    command: redis-server --appendonly yes --save 60 1 --loglevel warning
    expose:
      - "6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 5s
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1024M
        reservations:
          cpus: '0.25'
          memory: 256M
    networks:
      - erppreflight-net

  # ----------------------------------------------------------------------------
  # MinIO S3-Compatible Object Store (Quarantine, Clean & Reports Buckets)
  # ----------------------------------------------------------------------------
  minio:
    image: elestio/minio:latest
    container_name: erppreflight-minio
    restart: unless-stopped
    command: server /data --console-address ":9001"
    expose:
      - "9000"
      - "9001"
    environment:
      MINIO_ROOT_USER: ${S3_ACCESS_KEY:-minioadmin}
      MINIO_ROOT_PASSWORD: ${S3_SECRET_KEY:-minioadmin}
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:9000/minio/health/live || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 1024M
        reservations:
          cpus: '0.25'
          memory: 256M
    networks:
      - erppreflight-net

  # ----------------------------------------------------------------------------
  # Python 3.13 FastAPI Stateless Analysis Microservice (18 SAP Engines + MFS)
  # ----------------------------------------------------------------------------
  analysis-python:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.analysis
    container_name: erppreflight-analysis
    restart: unless-stopped
    expose:
      - "8000"
    environment:
      PORT: 8000
      PYTHONUNBUFFERED: 1
      SERVICE_VERSION: "1.0.0"
      CORS_ORIGINS: "http://api:3001,http://web:3000,http://localhost:3001,http://localhost:3000,https://erppreflight.com,https://api.erppreflight.com"
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:8000/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 15s
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2048M
        reservations:
          cpus: '0.5'
          memory: 512M
    networks:
      - erppreflight-net

  # ----------------------------------------------------------------------------
  # NestJS 11 Core API SaaS Backend (Auth, Ingestion, Tenancy, BullMQ, Reports)
  # ----------------------------------------------------------------------------
  api:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.api
    container_name: erppreflight-api
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      analysis-python:
        condition: service_healthy
    expose:
      - "3001"
    environment:
      NODE_ENV: production
      PORT: 3001
      API_PORT: 3001
      AUTO_MIGRATE: "true"
      STRICT_MIGRATIONS: ${STRICT_MIGRATIONS:-false}
      DATABASE_URL: postgres://${POSTGRES_USER:-erppreflight}:${POSTGRES_PASSWORD:-erppreflight_secret}@postgres:5432/${POSTGRES_DB:-erppreflight}
      REDIS_URL: redis://redis:6379
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_PASSWORD: ${REDIS_PASSWORD:-}
      ANALYSIS_SERVICE_URL: http://analysis-python:8000
      S3_ENDPOINT: http://minio:9000
      S3_ACCESS_KEY: ${S3_ACCESS_KEY:-minioadmin}
      S3_SECRET_KEY: ${S3_SECRET_KEY:-minioadmin}
      S3_REGION: ${S3_REGION:-us-east-1}
      S3_BUCKET_QUARANTINE: ${S3_BUCKET_QUARANTINE:-erppreflight-quarantine}
      S3_BUCKET_CLEAN: ${S3_BUCKET_CLEAN:-erppreflight-clean}
      S3_BUCKET_REPORTS: ${S3_BUCKET_REPORTS:-erppreflight-reports}
      JWT_SECRET: ${JWT_SECRET:-super-secret-jwt-key-must-be-at-least-32-chars-long-erppreflight}
      JWT_EXPIRES_IN: ${JWT_EXPIRES_IN:-7d}
      CORS_ORIGIN: ${CORS_ORIGIN:-https://erppreflight.com,http://localhost:3000}
      CLAMAV_HOST: ${CLAMAV_HOST:-clamav}
      CLAMAV_PORT: ${CLAMAV_PORT:-3310}
      CLAMAV_MOCK_MODE: ${CLAMAV_MOCK_MODE:-true}
      MASTER_ENCRYPTION_KEY: ${MASTER_ENCRYPTION_KEY:-erppreflight-default-master-key-32-chars-minimum-abcdef}
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:3001/health/liveness || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 25s
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2048M
        reservations:
          cpus: '0.5'
          memory: 512M
    networks:
      - erppreflight-net
    labels:
      - "coolify.managed=true"
      - "traefik.enable=true"
      - "traefik.http.routers.erppreflight-api.rule=Host(`api.erppreflight.com`)"
      - "traefik.http.routers.erppreflight-api.entrypoints=websecure"
      - "traefik.http.routers.erppreflight-api.tls=true"
      - "traefik.http.routers.erppreflight-api.tls.certresolver=letsencrypt"
      - "traefik.http.services.erppreflight-api.loadbalancer.server.port=3001"

  # ----------------------------------------------------------------------------
  # Next.js 15 Web Application (App Router, Base UI, TanStack Suite)
  # ----------------------------------------------------------------------------
  web:
    build:
      context: .
      dockerfile: infra/docker/Dockerfile.web
    container_name: erppreflight-web
    restart: unless-stopped
    depends_on:
      api:
        condition: service_healthy
    expose:
      - "3000"
    environment:
      NODE_ENV: production
      PORT: 3000
      NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-https://api.erppreflight.com}
      NEXT_TELEMETRY_DISABLED: 1
    healthcheck:
      test: ["CMD-SHELL", "wget --no-verbose --tries=1 --spider http://localhost:3000/api/health || exit 1"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 20s
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 2048M
        reservations:
          cpus: '0.5'
          memory: 512M
    networks:
      - erppreflight-net
    labels:
      - "coolify.managed=true"
      - "traefik.enable=true"
      - "traefik.http.routers.erppreflight-web.rule=Host(`erppreflight.com`, `www.erppreflight.com`, `b1jbemmdf9uioi0w2fdnnlb2.187.124.174.130.sslip.io`)"
      - "traefik.http.routers.erppreflight-web.entrypoints=websecure"
      - "traefik.http.routers.erppreflight-web.tls=true"
      - "traefik.http.routers.erppreflight-web.tls.certresolver=letsencrypt"
      - "traefik.http.routers.erppreflight-web-http.rule=Host(`erppreflight.com`, `www.erppreflight.com`, `b1jbemmdf9uioi0w2fdnnlb2.187.124.174.130.sslip.io`)"
      - "traefik.http.routers.erppreflight-web-http.entrypoints=http"
      - "traefik.http.services.erppreflight-web.loadbalancer.server.port=3000"

# ------------------------------------------------------------------------------
# Persistent Storage Volumes
# ------------------------------------------------------------------------------
volumes:
  postgres_data:
    name: erppreflight_postgres_data
  redis_data:
    name: erppreflight_redis_data
  minio_data:
    name: erppreflight_minio_data

# ------------------------------------------------------------------------------
# Isolated Internal Docker Network
# ------------------------------------------------------------------------------
networks:
  erppreflight-net:
    name: erppreflight-network
    driver: bridge
